import { UploadContainer, ButtonUpload, UploadContent } from './styled';

export function Upload() {
    return (
        <UploadContainer>
            <UploadContent>
                <ButtonUpload> Upload de imagem </ButtonUpload>
            </UploadContent>
        </UploadContainer>
    );
}
